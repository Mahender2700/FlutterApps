import 'package:flutter/material.dart';

class DistrictWiseQuizScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('District Wise Quiz Screen'),
      ),
    );
  }
}
