import 'package:flutter/material.dart';

class MixedQuizScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mixed Quiz Screen'),
      ),
    );
  }
}
